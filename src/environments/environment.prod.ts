export const environment = {
    production: true,
    hmr       : false,
    api_url   : 'https://api.ultimateteamsuite.com/CLIENT_NAME/v1',
    version: require('../../package.json').version.toString()
};
